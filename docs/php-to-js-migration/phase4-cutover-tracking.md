# Phase 4 — Cutover Tracking

Per [php-to-js-migration-checklist.md](../php-to-js-migration-checklist.md).
Tracked by module rather than HTTP route where there's no route yet — see
[00-phase0-inventory.md](00-phase0-inventory.md) for why (no Node backend
exists yet; these are library-level ports).

| Route/Feature | Functions migrated | Parity tests passing | Traffic cut over? | Notes |
|---|---|---|---|---|
| `lib/services` | `GedcomService.canonicalTag`, `readLatitude`, `readLongitude` (+ private `readDegrees` helper) from task 13; `RomanNumeralsService.numberToRomanNumerals`, `RomanNumeralsService.romanNumeralsToNumber` from task 14 — 6/6 total | ✅ 81/81 (`npm test` — 43 from task 13 + 38 from task 14); ✅ task 14 indirect coverage via `parity_date_french_hijri_jalali_jewish.test.js` (FrenchDate's `formatLongYear()` calls `numberToRomanNumerals`); ✅ GedcomService bridge covered by `tests/Feature/GedcomServiceBridgeTest.php` | ⚠️ `GedcomService` bridged but not enabled — `app/Services/GedcomService.php`'s `canonicalTag()`/`readLatitude()`/`readLongitude()` now route through `server/migration-service.mjs` *when* `WEBTREES_GEDCOM_SERVICE_URL` is set, with automatic fallback to native PHP. Unset (0%) in every environment today — see [phase3-gedcom-service-bridge.md](phase3-gedcom-service-bridge.md). `RomanNumeralsService` is N/A by design — not bridged (no PHP call site, internal JS-only caller) | Task 13 complete: `GedcomService` fully ported — post-landing review found `isNumeric()`'s first draft (`!isNaN(Number(x))`) diverges from PHP's `is_numeric()` for hex strings and `'Infinity'`/`'NaN'` literals (`Number()` parses them, PHP doesn't); fixed with a proper numeric-string regex, 5 golden cases added. Task 14 complete: promoted ad-hoc helper from task 11's `lib/date/roman-numerals.js` to formal service, added `romanNumeralsToNumber()` method. GedcomService's Phase 3 bridge landed after task 13 (see phase3-gedcom-service-bridge.md) — real callers are `GedcomImportService`, `Fact`. Flipping the env var on in a real deployment is a separate, later decision, same posture as Soundex/SurnameTradition. |
| `lib/soundex` | `russell`, `compare`, `daitchMokotoff` (+ bridging helpers `strtoupper`, `textScript`) — 5/5 across tasks 1-2 | ✅ 61/61 (`npm test`); ✅ bridge itself covered by `tests/Feature/SoundexServiceBridgeTest.php` | ⚠️ Bridged but not enabled — `app/Soundex.php`'s 4 real callers (`GedcomImportService`, `SearchService`, `BranchesListModule`, `Place`) now route through `server/migration-service.mjs` *when* `WEBTREES_SOUNDEX_SERVICE_URL` is set, with automatic fallback to native PHP. Unset (0%) in every environment today — see [phase3-soundex-bridge.md](phase3-soundex-bridge.md) | Tasks 1 and 2 (the ports) and the Phase 3 bridge are all complete. Flipping the env var on in a real deployment — i.e. actually cutting traffic — is a separate, later decision; known risk to weigh first: no request batching yet, so bulk GEDCOM import pays one HTTP round-trip per name. |
| `lib/comparators` | `TagComparator.order`, `TagComparator.byOrder` (task 3); `Date::compare` (task 12 Part A); `FactComparator.byDate`, `byType`, `typeOrder`, `effectiveTag` (task 12 Part B) — 6/6 across tasks 3 & 12 | ✅ 52/52 (`npm test` — 14 from task 3 + 10 from date-compare + 28 from fact-comparator) | N/A by design — not bridged | Task 3 complete (TagComparator). Task 12 complete: `Date::compare` ported (Part A, pure JD arithmetic); `FactComparator` fully ported (Part B — the real payoff this row was unblocking, combining Date-compare + TagComparator to sort facts by date, then by type). **Not bridged to live call sites.** FactComparator's real callers are `app/GedcomRecord.php:577-578`, `app/Services/FactSortService.php`, `app/Services/GedcomEditService.php`, `app/Module/TimelineChartModule.php` — all live, non-deprecated, high-traffic. Bridging would have real payoff if decided later; bridge architecture: same pattern as TagComparator (route in `server/migration-service.mjs`, short-circuit with fallback in PHP). Out of scope for this task (port + parity test only). |
| `lib/surname-tradition` | All 9 registered traditions (`Default` + `Patrilineal`, `Paternal`, `Matrilineal`, `Icelandic`, `Lithuanian`, `Polish`, `Portuguese`, `Spanish`) — 9/9, tasks 4-5 | ✅ 146/146 (`npm test`); ✅ bridge itself covered by `tests/Feature/SurnameTraditionServiceBridgeTest.php` | ⚠️ Bridged but not enabled — `SurnameTraditionFactory`'s 5 real callers (the "add family member" HTTP request handlers) now route through `server/migration-service.mjs` *when* `WEBTREES_SURNAME_TRADITION_SERVICE_URL` is set, via a `BridgedSurnameTradition` decorator, with automatic fallback to native PHP. Unset (0%) in every environment today — see [phase3-surname-tradition-bridge.md](phase3-surname-tradition-bridge.md) | Tasks 4 and 5 (the ports) and the Phase 3 bridge are all complete. `server/migration-service.mjs` is the same process Soundex bridges to (renamed from `soundex-service.mjs`) — the two bridges are independently toggled via separate env vars but typically point at the same running service. |
| `lib/ext-calendar` | `JulianCalendar`, `GregorianCalendar`, `FrenchCalendar`, `ArabicCalendar`, `PersianCalendar`, `JewishCalendar` (full `CalendarInterface` contract each, + `easterDays()`/`mod()`/`numberToHebrewNumerals()` where applicable) — **6 of 6 calendars — cluster complete**, tasks 7, 9 & 10 | ✅ 274/274 (`npm test`; 106 from task 7 + 70 from task 9 + 98 from task 10) | N/A by design — not bridged | `vendor/fisharebest/ext-calendar`, not `app/`. Now consumed by `lib/date` (task 8), so it has an internal JS caller even though no PHP code bridges to it yet. Bugs/artifacts found and faithfully reproduced along the way: task 7's `easterDays()` float-`lunar` bug; task 9's `PersianCalendar.jdToYmd()` negative-year non-round-trip (a limitation the PHP source documents itself) and `mod()`'s `-0` vs PHP's plain `int(0)` (fixed); task 10 deliberately excluded ~250 lines of `JewishCalendar`'s ISO-8859-8/`jdToHebrew()` surface, confirmed unused anywhere in webtrees by grep. |
| `lib/date` | `AbstractCalendarDate`, `AbstractGregorianJulianDate`, `GregorianDate`, `JulianDate`, `RomanDate`, `FrenchDate`, `HijriDate`, `JalaliDate`, `JewishDate` — **7 of 7 `app/Date/*.php` classes — cluster complete**, tasks 8 & 11 | ✅ 274/274 (`npm test`; 73 from task 8 + 201 from task 11) | N/A by design — not bridged | Task 11 completes the `app/Date/` wrapper layer. An HTTP bridge was investigated and deliberately **not built**: `AbstractCalendarDate` objects returned by `Date::minimumDate()`/`maximumDate()` escape into `Age`, 8 `CensusColumn*.php` files, etc., which call further methods directly — a bridge inside `Date.php` wouldn't cover those. Call volume is real (~150+ `Date` objects, 400-600 method calls on a 50-row list page) — a chatty per-call or per-object bridge is a non-starter. The one cleanly-bridgeable method (`display()`) wouldn't let PHP retire any calendar code, since native objects still have to exist for every other caller — pure added latency, no offsetting removal. Full reasoning and grepped call-site data: [phase3-date-bridge-design.md](phase3-date-bridge-design.md). **Update, task 17:** GEDCOM date-string parsing (`CalendarDateFactory::make()` + `Date::__construct()`'s qualifier/dual-date regexes) is now ported too — see the `lib/gedcom-date` row below — so this cluster's real-call-site input shape is now fully covered in JS, even though the bridge decision above is unchanged. `calendarUrl()` deliberately not ported (pure PHP routing plumbing). `convertToCalendar()` still only supports `'gregorian'`/`'julian'` targets — no caller needs the other 4 yet. |
| `lib/age` | `Age.__construct()`, `Age.toString()`, `Age.ageDays()`, `Age.ageYears()`, `Age.ageYearsString()` — 5/5, task 15 | ✅ 8/8 (`npm test`; all in `parity_age.test.js` — cases covering all output branches: years > 0, months > 0, days > 0, is_exact fallback, zero fallback, negative age, invalid date, precision handling) | N/A by design — not bridged | Task 15 complete: `Age` is a leaf consumer of the `lib/date` calendar engine — it calculates age difference between two GEDCOM dates for "age at event" display. The port accepts shim objects (`{ minimumDate, maximumDate, isOK }`) shaped like webtrees Date wrappers but backed by real `AbstractCalendarDate` instances from `lib/date/`. **Not bridged to live call sites.** `Age` has ~14 call sites (8 `CensusColumnAge*.php` files, `IndividualPage.php`, fact-display templates, etc.), but building an HTTP bridge would add per-computation latency with no PHP-code removal payoff (Date objects must still exist for other callers). Bridge architecture if needed later: single POST route returning all 4 output values (`ageDays`, `ageYears`, `ageYearsString`, `toString`), reducing payload vs. per-method calls. Out of scope (port + parity test only). |
| `lib/encodings` | All 12 concrete `app/Encodings/*.php` classes (ANSEL, ASCII, CP437, CP850, ISO-8859-1/2, MacOS Roman, UTF-8, UTF-16BE/LE, Windows-1250/1251/1252) + both abstract base classes + `EncodingFactory.detect()`/`make()`/`list()` — full cluster, task 16 | ✅ 2,875/2,875 (`npm test`) across 6 parity test files | N/A by design — not bridged | Task 16 complete. Byte-string vs. real-Unicode-text convention documented in `lib/encodings/abstract-encoding.js`; `UTF8` is the one deliberate exception (byte string both ways — see [task-16-encodings.md](task-16-encodings.md)). **Three real PHP bugs found and faithfully reproduced (not fixed, per explicit decision):** (1) `AbstractUTF16Encoding::fromUtf8()` has an operator-precedence bug that always zeroes non-ASCII characters — live via GEDCOM export to UTF-16 (`ExportGedcomClient.php`, `ClippingsCartModule.php`); (2) `AbstractUTF16Encoding::toUtf8()` treats the entire Latin-1 Supplement block (U+0080-U+00FF, ordinary accented letters) as invalid — live via UTF-16-encoded GEDCOM import; (3) `MacRoman`'s table has no entry for byte 0xF0, causing an uncaught `TypeError` in real PHP if converted onward. All three documented in full in [task-16-encodings.md](task-16-encodings.md). Only real PHP caller of these classes is `GedcomEncodingFilter` (a `php_user_filter` stream filter) — unbridgeable, same posture as `RomanNumeralsService`; this port is a foundation for a future JS-side GEDCOM pipeline, not a cutover candidate today. |
| `lib/gedcom-date` | `CalendarDateFactory.make()` (as `makeCalendarDate()`) + `Date`'s constructor and every non-display method (`minimumDate`, `maximumDate`, `minimumJulianDay`, `maximumJulianDay`, `julianDay`, `addYears`, `isOK`, `gregorianYear`, `compare`) — task 17 | ✅ 79/79 (`npm test`; `parity_calendar_date_factory.test.js` + `parity_gedcom_date.test.js`) | N/A by design — not bridged | Task 17 complete: closes the gap flagged in task 11's `phase3-date-bridge-design.md` — GEDCOM date **strings** can now be parsed entirely in JS via `new GedcomDate(dateString)`, not just hand-built `[year, month, day]` arrays. Same bridge reasoning as `lib/date` (objects escape into unrelated callers) — not bridged. One PCRE-anchoring asymmetry found in the "unambiguous Julian" regex, investigated and confirmed harmless in practice (not silently fixed) — see [task-17-gedcom-date.md](task-17-gedcom-date.md). Wiring `GedcomDate` into `Age` (task 15) needs a one-line adapter since `Age`'s shim reads properties, not methods — documented and verified end-to-end, not just asserted. |

Review this table weekly. If a row hasn't moved in two weeks, that's the
signal something's blocked — usually a Phase 3 bridging decision — surface
it rather than letting it sit.
